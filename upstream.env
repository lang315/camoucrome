# The Chromium revision this change set is generated against.
# scripts/apply.sh refuses any other HEAD unless CAMOU_PIN_OVERRIDE=1 is set
# (the rebase drill sets it deliberately; nothing else should).
CHROMIUM_REV=a727b5780538c3067f621494e85de94b4791db4f
