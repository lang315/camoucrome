{
  "$comment": [
    "The registry of cross-surface invariants.",
    "",
    "Source of truth for the fingerprint generator's post-generation",
    "self-check and for the generated mutation tests. C++ compiles against",
    "additions/camoucfg/invariants.h, which SP6a will generate from this file;",
    "until then the two are kept in step by hand and",
    "CoherenceValidatorTest.RegistryMatchesGeneratedHeader asserts they agree.",
    "",
    "Entries are added by the sub-project that owns the keys they constrain.",
    "An SP is not complete until its invariants are here with a passing",
    "mutation test -- an entry without one is documentation, not enforcement,",
    "and reads as coverage while providing none."
  ],
  "invariants": [
    {
      "id": "ua-os-family-agrees",
      "keys": [
        "ua:osInfo",
        "ua:platform"
      ],
      "relation": "same-os-family",
      "policy": "repair",
      "repair": "set ua:platform to the canonical UA-CH platform for the OS family of ua:osInfo",
      "why": [
        "ua:osInfo lands in the user-agent string. ua:platform lands in",
        "navigator.userAgentData.platform and in the Sec-CH-UA-Platform request",
        "header. A page reads both without effort, and a Windows user agent",
        "beside a Linux platform is a stronger signal than either value would",
        "be if it were simply honest.",
        "",
        "ua:osInfo wins because it is the more visible of the two, so repairing",
        "the other changes less of what pages actually see.",
        "",
        "A key that is absent, or present but unrecognised, constrains nothing.",
        "This entry catches values that CONTRADICT one another; an absent key",
        "constrains nothing for this relational entry because the incoherence",
        "an absent partner creates -- one channel spoofed, the other silently",
        "reporting the real OS -- is reported by the startup diagnostic in",
        "content/browser/browser_main_loop.cc, not here. An unrecognised value",
        "is a per-key type question rather than a relational one.",
        "",
        "SP1a ships this incoherence today and names SP5a as its owner -- see",
        "the comment above ConfigPresentsUserAgentIdentity in",
        "patches/sp1a-ua-producer.patch."
      ]
    },
    {
      "id": "screen-avail-width-fits",
      "keys": [
        "screen.width",
        "screen.availWidth"
      ],
      "relation": "fits-within",
      "policy": "repair",
      "repair": "lower screen.availWidth to screen.width",
      "why": [
        "The available screen area is the display minus the OS chrome",
        "(taskbar, dock, menu bar), so it is physically a subset of the",
        "display: screen.availWidth <= screen.width on every real machine,",
        "with equality in the no-taskbar / fullscreen-kiosk case. A",
        "configuration reporting availWidth > width describes a work area",
        "larger than the screen containing it -- a state no real device",
        "produces, and a contradiction a detector reads by comparing two",
        "screen properties.",
        "",
        "screen.width is authoritative: it is the display the work area is",
        "carved out of, so the work area is what gives when they disagree.",
        "Both keys are read as GetUint32, the getter sp4a-screen consumes",
        "them with; a value that is absent, or present with the wrong type",
        "(GetUint32 wants a non-negative integer), constrains nothing.",
        "",
        "Absent-key posture matches ua-os-family-agrees: this entry catches a",
        "contradiction between two configured values. A lone spoofed key",
        "(availWidth set, width real) is one channel spoofed with the other",
        "reporting the truth, which is the startup diagnostic's to report, not",
        "a relational entry's -- with one value configured there is no",
        "disagreement to demonstrate. An out-of-range single value is SP5b's",
        "to reject."
      ]
    },
    {
      "id": "screen-avail-height-fits",
      "keys": [
        "screen.height",
        "screen.availHeight"
      ],
      "relation": "fits-within",
      "policy": "repair",
      "repair": "lower screen.availHeight to screen.height",
      "why": [
        "The height axis of screen-avail-width-fits: screen.availHeight <=",
        "screen.height for the same physical reason, and the same absent-key,",
        "wrong-type and authoritative-key posture applies. Split per axis",
        "rather than folded into one entry because Relation is a binary",
        "comparison over two keys, and the two axes are independent",
        "contradictions -- a config can violate one without the other."
      ]
    },
    {
      "id": "navigator-platform-matches-os",
      "keys": [
        "ua:osInfo",
        "navigator.platform"
      ],
      "relation": "same-platform-bucket",
      "policy": "repair",
      "repair": "set navigator.platform to the canonical reduced platform for the OS family of ua:osInfo",
      "why": [
        "ua:osInfo lands in the user-agent string; navigator.platform is its",
        "own JS property. A page reads both, and a Windows user agent beside",
        "navigator.platform === 'MacIntel' is a textbook OS tell. The reduced",
        "User-Agent freezes one canonical platform per OS -- Win32, MacIntel,",
        "Linux x86_64, Linux armv81 -- so the correct value is a function of",
        "the claimed OS, and any other explicit value contradicts it.",
        "",
        "A canonical-STRING compare, not a same-OS-family one: Linux and",
        "ChromeOS both report 'Linux x86_64' under UA reduction, so they share",
        "one platform bucket. A family-level check would wrongly flag a ChromeOS",
        "claim beside 'Linux x86_64' as a mismatch.",
        "",
        "ua:osInfo is authoritative, so navigator.platform is the value the log",
        "names as wrong. This is the EXPLICIT-value complement of the SP1b",
        "navigator.platform derive, which fills the key from the claimed OS only",
        "when it is ABSENT (an explicit value wins over the derive). The two are",
        "disjoint: the derive fires on an absent key, this entry on a present",
        "one. Same absent-key and unrecognised-value posture as",
        "ua-os-family-agrees; a ua:platform-only OS claim (no ua:osInfo) is out",
        "of this entry's reach, and the startup diagnostic warns on it instead.",
        "",
        "Deferred to SP5a by the derive's measurement doc",
        "(2026-09-07-navigator-platform-derive.md) as kSamePlatformBucket."
      ]
    },
    {
      "id": "navigator-language-heads-languages",
      "keys": [
        "navigator.languages",
        "navigator.language"
      ],
      "relation": "list-head-equals",
      "policy": "repair",
      "repair": "set navigator.language to navigator.languages[0]",
      "why": [
        "Real Chrome derives navigator.language from the same accept-languages",
        "pref that fills navigator.languages, so language is always languages[0].",
        "A page reads both for free; a configuration where they disagree",
        "describes a browser that cannot exist. The list is authoritative",
        "because it carries more information (the whole preference order);",
        "repairing the scalar changes less of what pages see.",
        "Either key absent constrains nothing: SP1b falls back to the real",
        "value for the missing one, and the real values agree by construction."
      ]
    },
    {
      "id": "locale-tag-matches-navigator-language",
      "keys": [
        "locale:tag",
        "navigator.language"
      ],
      "relation": "same-string",
      "policy": "repair",
      "repair": "set navigator.language to locale:tag",
      "why": [
        "locale:tag drives blink's LocaleController (Intl.*.resolvedOptions().locale,",
        "toLocaleString defaults); navigator.language is SP1b's JS getter. Real",
        "Chrome derives both from one preference, so Intl and navigator agree.",
        "sp4-tz-locale's core_initializer hook makes locale:tag win over",
        "navigator.language for Intl when both are set, so locale:tag is the",
        "authority here and the getter is repaired toward it.",
        "Exact, case-sensitive comparison on purpose: Chrome reports canonical",
        "BCP-47 tags (en-US, never en-us), so an operator's lower-case tag is",
        "itself a tell rather than a spelling this entry should forgive.",
        "Accept-Language is NOT covered: nothing in the tree derives the header",
        "from locale:tag (2026-09-01 tz-locale measurement §1), so a header/JS",
        "mismatch is a code-path gap for the generator, not a config invariant."
      ]
    },
    {
      "id": "webgl2-vendor-agrees-with-webgl",
      "keys": [
        "webGl:vendor",
        "webGl2:vendor"
      ],
      "relation": "same-string",
      "policy": "repair",
      "repair": "set webGl2:vendor to webGl:vendor",
      "why": [
        "A page can create a WebGLRenderingContext and a WebGL2RenderingContext",
        "side by side and read UNMASKED_VENDOR_WEBGL from each. On every real",
        "device both contexts run on the same GPU through the same ANGLE",
        "backend, so the strings are identical. The keys are separate so a",
        "profile MAY answer each independently (spec: 'free to answer each",
        "independently'), but when both are set they must agree. WebGL1 is",
        "authoritative only because every profile sets it first; either",
        "absent constrains nothing (the pairing check owns half-configured",
        "contexts, and the unset context reports the real GPU)."
      ]
    },
    {
      "id": "webgl2-renderer-agrees-with-webgl",
      "keys": [
        "webGl:renderer",
        "webGl2:renderer"
      ],
      "relation": "same-string",
      "policy": "repair",
      "repair": "set webGl2:renderer to webGl:renderer",
      "why": [
        "Same reasoning as webgl2-vendor-agrees-with-webgl for",
        "UNMASKED_RENDERER_WEBGL: one GPU, one backend, one string on both",
        "context types. A profile that names two different GPUs across the two",
        "contexts describes a machine that does not exist."
      ]
    },
    {
      "id": "webgl-renderer-backend-fits-os",
      "keys": [
        "ua:osInfo",
        "webGl:renderer"
      ],
      "relation": "renderer-backend-fits-os",
      "policy": "repair",
      "repair": "choose a webGl:renderer whose ANGLE backend runs on the claimed OS (Direct3D11 on Windows, Metal on macOS)",
      "why": [
        "Chrome's UNMASKED_RENDERER_WEBGL is ANGLE's renderer description, and",
        "it names the backend: 'ANGLE (NVIDIA, NVIDIA GeForce RTX 3060",
        "(0x00002504) Direct3D11 vs_5_0 ps_5_0, D3D11)' on Windows, 'ANGLE",
        "(Apple, ANGLE Metal Renderer: Apple M1, Unspecified Version)' on macOS,",
        "'ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero)",
        "(0x0000C0DE)), SwiftShader driver)' on this Linux build (measured",
        "2026-09-10 on the stock M153 chrome). Direct3D exists only on Windows",
        "and Metal only on Apple platforms, so a Direct3D renderer under a",
        "macOS user agent, or a Metal one under Windows, is an incoherence a",
        "page detects with two getParameter calls (spec §4.3: 'an Apple GPU",
        "string under a Windows user agent ends the session').",
        "Conservative on purpose: only the two tokens exclusive to one OS",
        "family fire. OpenGL and Vulkan renderers occur on Linux, Android and",
        "older macOS, so they constrain nothing, the same rule the registry",
        "applies to any unrecognised value. The renderer is resolved through",
        "GLRenderer(), the same helper the pairing check uses, so a renderer",
        "supplied via webGl:parameters[\"37446\"] is checked too.",
        "There is no canonical repair value; new_value is descriptive."
      ]
    }
  ]
}
